---
phase: quick-260806-k98
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(storefront)/products/[handle]/page.tsx
  - src/app/(storefront)/products/[handle]/page.test.tsx
  - src/lib/shopify/html-content.ts
  - src/lib/shopify/html-content.test.ts
  - src/components/product/product-form/product-form.tsx
  - src/components/product/product-form/product-form.test.tsx
  - src/components/product/bulk-savings/bulk-savings.tsx
  - src/components/product/bulk-savings/bulk-savings.test.tsx
autonomous: true
requirements: [PDP-PARITY-01, PDP-PARITY-02, PDP-PARITY-03, PDP-PARITY-04, PDP-PARITY-05]

must_haves:
  truths:
    - "A shopper reading a PDP sees the merchant-authored description exactly once — no derived Tasting/Ingredients/Packing accordions repeating Origin, Ingredients, Aroma, Serving Suggestion, Packaging, Storage, or Quality Control"
    - "No raw tag value (e.g. 'aco') and no placeholder filler copy ('...is being prepared for this product.') renders anywhere on the PDP"
    - "A shopper on an in-stock product sees 'In stock! Usually ships within 24 hours.'; on a sold-out selection they see the production sold-out line"
    - "Every displayable tag renders as a pill — a product with 8 displayable tags shows 8 pills"
    - "Bulk tier totals equal round-to-cents(unit x (1 - discount) x quantity): 11.47 base gives 54.48 / 103.23 / 201.87 / 389.98"
    - "A product with zero reviews shows a review line reading '0 Reviews' instead of hiding the line"
  artifacts:
    - path: "src/app/(storefront)/products/[handle]/page.tsx"
      provides: "PDP with description rendered once, uncapped tags, always-visible review line"
    - path: "src/components/product/product-form/product-form.tsx"
      provides: "Variant-driven availability message"
    - path: "src/components/product/bulk-savings/bulk-savings.tsx"
      provides: "Multiply-then-round tier totals"
    - path: "src/components/product/bulk-savings/bulk-savings.test.tsx"
      provides: "Regression test pinning production tier totals"
  key_links:
    - from: "src/components/product/product-form/product-form.tsx"
      to: "selectedVariant.availableForSale"
      via: "availability message branch"
      pattern: "Usually ships within 24 hours"
    - from: "src/components/product/bulk-savings/bulk-savings.tsx"
      to: "tier total display"
      via: "unrounded unit amount x quantity, rounded once"
      pattern: "toFixed\\(2\\)"
---

<objective>
Close five PDP parity defects found by direct browser comparison of staging against production (`https://www.teavision.com.au/products/black-assam-op1`). Production is the source of truth.

Purpose: the PDP currently duplicates every description field inside derived accordions, leaks raw tag values, hides availability messaging, silently drops tags past the sixth, mis-rounds bulk totals by cents, and hides the review line for zero-review products.

Output: PDP that matches production on description rendering, availability messaging, tag completeness, bulk tier totals, and review-count visibility.
</objective>

<execution_context>
@C:/Users/evang/AppData/Roaming/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/workflows/execute-plan.md
@C:/Users/evang/AppData/Roaming/Claude/local-agent-mode-sessions/614fd504-8566-46b2-8256-66ded77bb604/43ed32e3-053c-413f-843e-d49e1f00f5e1/rpm/plugin_019FPGEfUV8XSdonegN1mWDs/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@docs/conventions.md
@src/app/(storefront)/products/[handle]/page.tsx
@src/components/product/product-form/product-form.tsx
@src/components/product/bulk-savings/bulk-savings.tsx

<interfaces>
<!-- Extracted from the codebase. Use directly — no exploration needed. -->

src/lib/reviews/summary.ts (SHARED — do not change; four other call sites plus the PDP JSON-LD gate depend on its current strictness):
  export type ProductReviewSummary = { rating: number; reviewCount: number }
  export function getVisibleProductReviewSummary(summary: { rating?: number; reviewCount?: number }): ProductReviewSummary | null
  Returns null unless rating > 0 && rating <= 5 && Number.isInteger(reviewCount) && reviewCount > 0.
  Other call sites: src/components/collection/product-card/product-card.tsx,
  src/app/(storefront)/products/[handle]/_components/related-products.tsx,
  src/app/(storefront)/collections/[handle]/_components/results.tsx,
  src/app/api/products/[handle]/quick-view/route.ts.

src/lib/shopify/html-content.ts:
  export function sanitizeShopifyCompactHtml(html: string): string   // KEEP — renders the PDP description
  export type ProductDescriptionDetails = { aroma?, ingredients?, origin?, packaging?, qualityControl?, servingSuggestion?, storage?, warning? }   // DELETE
  const PRODUCT_DESCRIPTION_LABELS = [...]                                                                                                          // DELETE
  export function extractProductDescriptionDetails(html: string): ProductDescriptionDetails                                                         // DELETE
  plainTextFromHtml(...) is used by other exports in this file — KEEP it.

src/components/product/product-form/product-form.tsx (existing local state, already computed):
  const selectedVariant = variants.find((v) => v.id === selectedVariantId)
  const canAddToCart = selectedVariant?.availableForSale === true

src/components/product/bulk-savings/bulk-savings.tsx (current, defective math):
  function parseAmount(money: Money): number
  function getTierPrice(tier: BulkPricingTier, basePrice: Money): Money | null   // returns (base * (1 - pct/100)).toFixed(2)
  function getTotalPrice(price: Money, quantity: number): Money                  // multiplies the ALREADY-ROUNDED unit price
  type BulkPricingTier = { minimumQuantity: number; discountPercent?: number; price?: Money; label?: string }
</interfaces>

<production_reference>
Verbatim strings taken from the production Liquid theme
(`../teavision-theme/locales/en.default.json`, rendered into `#variant-inventory`
by `assets/scripts.js.liquid` lines 1277-1291, positioned after the
`<div class="rte" itemprop="description">` block in `sections/product.liquid` line 161-179):

  products.product.product_is_avalable → "In stock! Usually ships within 24 hours."
  products.product.product_is_sold_out → "Sorry! This product is currently out of stock."

Production bulk totals for base unit price $11.47 (browser-verified):
  Buy 5  (5% off)  → unit $10.90, total $54.48    (11.47 * 0.95 * 5  = 54.4825)
  Buy 10 (10% off) → unit $10.32, total $103.23   (11.47 * 0.90 * 10 = 103.23)
  Buy 20 (12% off) → unit $10.09, total $201.87   (11.47 * 0.88 * 20 = 201.872)
  Buy 40 (15% off) → unit $9.75,  total $389.98   (11.47 * 0.85 * 40 = 389.98)
Production rounds the TOTAL once; staging rounds the unit first and multiplies, so it drifts by cents.
</production_reference>

<known_conflict>
`docs/launch/franz-my-tickets.md` TKT-02520 / TKT-02521 record that Lucas was "happy to keep"
the derived accordions, and TKT-02520 asks for a category-group brewing method sourced from
Shopify tags supplied by Lucas. That future feature is NOT what the current accordions deliver —
they re-parse the description and duplicate it. This plan removes the derived accordions per the
current owner directive (production parity, no duplicated content). If a brewing-method block is
revisited later it must be driven by Lucas-supplied per-category data, not description re-parsing.
Record this in the SUMMARY; do not attempt to build the category-group feature here.
</known_conflict>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove derived accordions, uncap tags, always show the review line</name>
  <files>src/app/(storefront)/products/[handle]/page.tsx, src/app/(storefront)/products/[handle]/page.test.tsx, src/lib/shopify/html-content.ts, src/lib/shopify/html-content.test.ts</files>
  <behavior>
    In `page.test.tsx` (update the existing suites rather than appending a parallel suite):
    - Rendering a product whose descriptionHtml contains Origin/Ingredients/Packaging/Storage/Quality Control/WARNING labels produces ZERO `<details>` elements, and each of those field values appears exactly once in the HTML.
    - The rendered HTML does not contain "Tasting", "Ingredients &amp; certification", "Packing, shipping", "Brewing guidance is being prepared", "have not yet been supplied", or a "Certifications" table row.
    - A product with 8 displayable tags renders 8 tag pills (drop the old 6-pill expectation if one exists); `Package_*` tags remain hidden via the existing `formatTag` rules.
    - Zero-review product: HTML contains "0 Reviews" AND the Product JSON-LD still has no `aggregateRating` (this is the Phase 18-04 invariant — keep it).
    - Reviewed product (rating 4.7, reviewCount 12): the existing "4.7 · 12 reviews" line and the `aggregateRating` node both remain.
  </behavior>
  <action>
    Defect 1 — remove derived accordions from `page.tsx`:
    delete the `SpecDisclosure` type, the `descriptionDetails` const, `ingredientRows`, `packingRows`, `certificationTags`, the `specDisclosures` array, the whole `<div className="mt-8">…{specDisclosures.map(…)}…</div>` disclosure block, the now-unused `getCertificationLabel` helper, and the now-unused `ChevronDown` and `extractProductDescriptionDetails` imports. Keep `sanitizeShopifyCompactHtml`, `formatTag`, `getCategoryLabel`, `getBadgeVariant`, `getMetaSegments`, and the existing `RichText` description block — the description must still render once, above the buy panel. Re-check the tag-pill wrapper's `mt-*` spacing after the disclosure block disappears so the pills do not collide with the buy panel; keep the existing token classes and `cn()` usage.

    Then delete the now-dead lib code in `src/lib/shopify/html-content.ts`: `ProductDescriptionDetails`, `PRODUCT_DESCRIPTION_LABELS`, and `extractProductDescriptionDetails` (confirm with grep that `page.tsx` and `html-content.test.ts` were the only consumers before deleting). Keep `plainTextFromHtml` and every other export. Remove the corresponding `describe('extractProductDescriptionDetails', …)` block from `html-content.test.ts`.

    Defect 3 — tag cap: in the tag-pill block, change `visibleTags.slice(0, 6).map(...)` to `visibleTags.map(...)`.

    Defect 5 — zero reviews: keep `getVisibleProductReviewSummary` untouched and keep gating the JSON-LD `aggregateRating` on it. For the visible line, derive page-local display values from `productReviewSummary`: a numeric rating (0 when absent/invalid) and an integer count (0 when absent/invalid). Render the star row unconditionally: `StarRating` with the display rating, then the count text. When the display count is 0 render exactly `0 Reviews` (production wording, capital R) with no rating prefix; when it is greater than 0 keep today's `{rating.toFixed(1)} · {count.toLocaleString()} review|reviews` wording. Do not introduce a new shared helper for this — it is PDP-local presentation.

    Prohibited: changing `src/lib/reviews/summary.ts`, loosening the JSON-LD aggregateRating gate, or adding placeholder/"coming soon" copy anywhere.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/lib/shopify/html-content.test.ts && pnpm exec vitest run page.test</automated>
  </verify>
  <done>No `<details>` blocks or derived spec tables render on the PDP; description fields appear once; all displayable tags render; zero-review products show "0 Reviews" while JSON-LD aggregateRating stays absent; `extractProductDescriptionDetails` no longer exists in the codebase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the production availability message to the product form</name>
  <files>src/components/product/product-form/product-form.tsx, src/components/product/product-form/product-form.test.tsx</files>
  <behavior>
    - With a selected variant where `availableForSale` is true, the form renders exactly "In stock! Usually ships within 24 hours."
    - With a selected variant where `availableForSale` is false, the form renders "Sorry! This product is currently out of stock." and does not render the in-stock line.
    - Selecting a sold-out variant on a product with mixed availability switches the line from the in-stock text to the sold-out text.
  </behavior>
  <action>
    Defect 2. In `product-form.tsx`, inside the existing price block — the `<div className="flex min-w-0 flex-col gap-3">` that wraps the `Price` + "selected pack" row — render an availability line immediately after that row, still guarded by `selectedVariant`. Use the already-computed `canAddToCart` (`selectedVariant?.availableForSale === true`) to choose between the two production strings quoted in `<production_reference>`. Style it as a single `<p>` using `type-caption` plus `text-brand` for in stock and `text-danger` for sold out, composed with `cn()` — the sold-out styling must reuse the same `text-danger` token already used by the quantity error paragraph. No live region and no `role="status"`: the existing quantity `role="status"` paragraph must stay the only live region in this form. Do not restructure the price block, the assurance row, or the add-to-cart button label ("Sold Out" stays).

    This lives in `ProductForm` rather than `page.tsx` because the message must follow the SELECTED variant, matching production's `#variant-inventory` behaviour; in the staging DOM `ProductForm` still sits after the description and before the tag pills, so production's placement order is preserved.

    Add the three behaviour cases to `product-form.test.tsx` following the existing test setup in that file. If `product-form.stories.tsx` has a sold-out story, no story changes are required — the new line renders from existing args.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/product/product-form/product-form.test.tsx</automated>
  </verify>
  <done>In-stock selections show "In stock! Usually ships within 24 hours."; sold-out selections show "Sorry! This product is currently out of stock."; the line updates on variant change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix bulk tier total rounding</name>
  <files>src/components/product/bulk-savings/bulk-savings.tsx, src/components/product/bulk-savings/bulk-savings.test.tsx</files>
  <behavior>
    New test file. With `basePrice` $11.47 AUD and percentage tiers 5/10/12/15 at quantities 5/10/20/40:
    - Displayed totals are exactly "Total $54.48", "Total $103.23", "Total $201.87", "Total $389.98".
    - Displayed per-unit prices stay rounded to cents ($10.90, $10.32, $10.09, $9.75).
    - A tier that carries an explicit `price` Money value (Shopify-authored, already in cents) totals `price x quantity` unchanged.
  </behavior>
  <action>
    Defect 4. Production multiplies the UNROUNDED discounted unit price by the tier quantity and rounds once; staging rounds the unit price first, so totals drift by cents ($404.00 vs $403.92 style errors).

    In `bulk-savings.tsx`, introduce a numeric `getTierUnitAmount(tier, basePrice): number | null` that returns `parseAmount(tier.price)` when `tier.price` exists, otherwise `baseAmount * (1 - tier.discountPercent / 100)` (still returning null when there is no `tier.price` and no `discountPercent`, or when `baseAmount <= 0`). Keep `getTierPrice` as the DISPLAY value built from that amount via `toFixed(2)`. Change the total so it is computed from the unrounded amount — `(unitAmount * tier.minimumQuantity).toFixed(2)` — instead of calling `getTotalPrice(tierPrice, …)` on the rounded Money. Leave `baseTotal` (struck-through full price) computed from `basePrice` as today, and leave tier ordering, best-value badge, labels, and the `In cart` fallback untouched.

    Create `bulk-savings.test.tsx` next to the component covering the behaviour cases above. Follow the render/query style already used in `src/components/product/product-form/product-form.test.tsx`.
  </action>
  <verify>
    <automated>pnpm exec vitest run src/components/product/bulk-savings/bulk-savings.test.tsx</automated>
  </verify>
  <done>Tier totals for base $11.47 read 54.48 / 103.23 / 201.87 / 389.98, matching production; per-unit displays unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopify Storefront API → PDP render | Merchant-authored `descriptionHtml` and tag strings cross into rendered HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-k98-01 | Tampering | `descriptionHtml` rendering in `page.tsx` | mitigate | Description keeps flowing through the existing `sanitizeShopifyCompactHtml` + `RichText` path; this plan removes a consumer, it must not add any new raw-HTML render path |
| T-k98-02 | Information disclosure | Tag pills with the `.slice(0, 6)` cap removed | mitigate | `formatTag` continues to drop `Package_*` internal tags before the pill list; uncapping renders more of the same already-public tag set only |
| T-k98-03 | Spoofing | Product JSON-LD `aggregateRating` | mitigate | Visible "0 Reviews" line stays decoupled from the JSON-LD gate; `getVisibleProductReviewSummary` is unchanged so no rating is claimed to search engines when reviewCount is 0 (Phase 18-04 invariant) |
| T-k98-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — no dependency surface changes |
</threat_model>

<verification>
1. `pnpm lint` passes (includes `scripts/check-tailwind-classes.mjs`).
2. `pnpm test:unit` passes with no skipped or stale accordion assertions left behind.
3. `grep -rn "extractProductDescriptionDetails" src` returns nothing.
4. `pnpm dev`, then compare `http://localhost:3000/products/black-assam-op1` against `https://www.teavision.com.au/products/black-assam-op1`:
   - description content appears once, zero accordions, no "aco" raw value, no placeholder filler
   - all 8 tags render as pills
   - review line present (0-review products read "0 Reviews")
   - availability line present and correct for the selected variant
   - on a product with 11.47 base pricing, bulk totals read 54.48 / 103.23 / 201.87 / 389.98
5. Spot-check 2-3 additional products (including one in-stock and one multi-variant) for the same five behaviours.
</verification>

<success_criteria>
- All five parity defects closed with production-verbatim strings and production-matching arithmetic.
- No dead code left: `extractProductDescriptionDetails`, `ProductDescriptionDetails`, `PRODUCT_DESCRIPTION_LABELS`, `getCertificationLabel`, `SpecDisclosure`, and the `ChevronDown` import are gone.
- `src/lib/reviews/summary.ts` and the JSON-LD `aggregateRating` gate are unchanged.
- `pnpm lint` and `pnpm test:unit` pass.
- Commit uses `fix(pdp): …` style with NO `Co-Authored-By` trailer.
</success_criteria>

<output>
Create `.planning/quick/260806-k98-fix-pdp-parity-issues-vs-production-acco/260806-k98-SUMMARY.md` when done, recording the TKT-02520/02521 accordion conflict noted in `<known_conflict>` and the now-stale accordion references in `docs/launch/seo-findings-validation-2026-07-13.md` (row 12) and `docs/launch/franz-my-tickets.md` as follow-up documentation debt.
</output>
</content>
</invoke>
